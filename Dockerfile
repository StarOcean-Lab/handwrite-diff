# Multi-stage Dockerfile for HandwriteDiff
# Build both frontend and backend with docker-compose

# This file is used by docker-compose.yml
# See Dockerfile.backend and Dockerfile.frontend for actual build stages
